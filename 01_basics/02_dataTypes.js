"use strict"; //treat all JS code as newer version

// alert("Hello") //we are using nodejs not browser


//Documentation MDN and tc39

//Primitive datatype
let name="Rahul"; //string
let age=21;  //Number (Having Range in a memory address)
let isLoggedIn=true; //boolean
//bigInt
//null ==> standalone value(representation of empty value)
//undefined ==>value didnt defined in a variable
//symbol (used more in react for finding the uniqueness in components)


// object
//console.log(typeof(name)) ==>return the type of the value
// typeof(null)==>object
// typeof(undefined)==>undefined